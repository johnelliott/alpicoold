[Unit]
Description=Alpicool Daemon
After=network.target

[Service]
EnvironmentFile=/etc/{{ servicename }}-env
Type=simple
ExecStart=/usr/local/bin/{{ servicename }}
Restart=always
RestartSec=1

[Install]
WantedBy=multi-user.target
